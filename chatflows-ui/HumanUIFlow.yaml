name: "HumanUIFlow"
description: "Human UI."

_target_: aiflows.base_flows.AtomicFlow.instantiate_from_default_config

input_interface:
  - "api_output" # since we mostly use it with ChatFlow
output_interface:
  - "human_input"
